// Operador ternário é uma forma abreviada e autoavaliada do IF ELSE

// Sintaxe:
// const resultado = condicao ? resultado_verdadeiro : resultado_falso;

const age = 32;

const age_people = age === 32 ? alert("Tem 32") : alert("Não tem 32");
